{
  "name": "cms",
  "version": "1.0.0",
  "description": "ZEDALBUMS Cloudflare Worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}